[[allowlist]]
description = "False positive: gcp-api-key in config.xml"
rules = ["gcp-api-key"]
paths = ["config.xml"]

[[allowlist]]
description = "False positive: gcp-api-key in www/index.html"
rules = ["gcp-api-key"]
paths = ["www/index.html"]

[[allowlist]]
description = "False positive: gcp-api-key in www/index.html"
rules = ["gcp-api-key"]
paths = ["www/index.html"]

[[allowlist]]
description = "False positive: gcp-api-key in www/index.html"
rules = ["gcp-api-key"]
paths = ["www/index.html"]

[[allowlist]]
description = "False positive: gcp-api-key in README.md"
rules = ["gcp-api-key"]
paths = ["README.md"]
